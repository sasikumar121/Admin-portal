<td class="swap">
	<a class="btn btn-swap {{ object.anons ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_field', {'field':'anons', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>