<td class="swap">
	<a class="btn btn-swap {{ object.sticked ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_field', {'field':'sticked', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>