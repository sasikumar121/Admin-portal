<td class="swap">
	<a class="btn btn-swap {{ object.hideDate ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_field', {'field':'hideDate', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>