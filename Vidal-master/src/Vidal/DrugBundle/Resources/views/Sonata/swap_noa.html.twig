<td class="swap">
	<a class="btn btn-swap {{ object.noa ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_field', {'field':'noa', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>