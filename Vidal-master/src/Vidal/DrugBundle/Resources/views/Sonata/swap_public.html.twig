<td class="swap">
	<a class="btn btn-swap {{ object.public ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_field', {'field':'public', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>