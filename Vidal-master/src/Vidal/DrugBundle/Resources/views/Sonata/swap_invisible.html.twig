<td class="swap">
	<a class="btn btn-swap {{ object.invisible ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_field', {'field':'invisible', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>