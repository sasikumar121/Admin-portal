<td class="swap">
	<a class="btn btn-swap {{ object.enabled ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_main', {'field':'enabled', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>